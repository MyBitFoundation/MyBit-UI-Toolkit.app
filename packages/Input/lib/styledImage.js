import styled from 'styled-components';

const StyledImage = styled.div`
  position: absolute;
  left: 100%;
  margin-left: 9px;
  top: 4px;

  img{
    width: 16px;
  }
`;

export default StyledImage;
