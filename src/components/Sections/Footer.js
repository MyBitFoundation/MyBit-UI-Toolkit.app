import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const LibraryFooter = () => (
    <Footer style={{ textAlign: 'center' }}>
        simple text footer should do for now
    </Footer>
)

export default LibraryFooter